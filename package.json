{
  "name": "search",
  "version": "0.1.0",
  "description": "Decentralized Search Protocol — search:// URI scheme over a peer-to-peer network",
  "main": "src/node.js",
  "bin": {
    "search": "./bin/search"
  },
  "scripts": {
    "start": "node bin/search",
    "test": "jest --runInBand",
    "lint": "eslint src/ bin/ test/"
  },
  "keywords": [
    "search",
    "decentralized",
    "p2p",
    "protocol"
  ],
  "author": "Nathan Sharp",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/test/**/*.test.js"
    ]
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
